```
Install all dependencies
python3 -m pip install flask flask-sqlalchemy flask-restx flask-httpauth marshmallow pyjwt

In the project root directory : to start flask server
python3 -m flask --app app --debug
```
